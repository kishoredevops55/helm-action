{{- $content := .Files.Get "input.yaml" -}}
{{ tpl $content . }}

{{ tpl (.Files.Get .Values.manifestFile) . }}

{{- range $path, $file := .Files.Glob "manifests/*.yaml" }}
{{ printf "%s" $file }}
{{- end }}

{{- range $path, $file := .Files.Glob "manifests/*.yaml" }}
{{ printf "%s" $file }}
{{- end }}

{{- /*
  Print .Values.manifest EXACTLY as-is.
  No toYaml, no extra keys, no reformatting.
*/ -}}
{{- if .Values.manifest }}
{{- printf "%s" .Values.manifest }}
{{- end }}

manifest: |

{{- /*
  Print .Values.manifest EXACTLY as-is.
  If .Values.manifest is empty or missing, output will be empty.
*/ -}}
{{- if .Values.manifest }}
{{- printf "%s" .Values.manifest }}
{{- end }}



